import { PostPreviewPipe } from './post-preview.pipe';

describe('PostPreviewPipe', () => {
  it('create an instance', () => {
    const pipe = new PostPreviewPipe();
    expect(pipe).toBeTruthy();
  });
});
